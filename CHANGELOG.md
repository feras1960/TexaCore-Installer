# 📋 TexaCore ERP — سجل التغييرات (Changelog)

## [v1.6.3] — 2026-07-16 — تحديث هوائي صامت + ودجت حالة التحديث في واجهة البرنامج والمثبّت

- **ودجت التحديث داخل واجهة البرنامج (ERP web UI):** مكوّن جديد في هيدر الواجهة (النسخة المحلية فقط) يعرض تنزيل التحديث بالخلفية (شريط تقدّم + نسبة) ثم «التحديث جاهز» مع زر إعادة تشغيل بتأكيد + «لاحقاً»؛ بعد التثبيت يعيد الاتصال تلقائياً. خامل تماماً في السحابة.
- **ودجت التحديث في نافذة المثبّت:** شريط رفيع تحت بطاقات الحالة (يظهر فقط عند وجود تحديث) بنفس الحالات، بأربع لغات (ع/EN/RU/UK).
- **API تحديث محلي:** `GET /api/update/status` و`POST /api/update/install` على منفذ 1960 (loopback فقط؛ محجوبة عن النفق السحابي وعن الأصول الغريبة CSRF)، تُغذّى من مسار الماك (MacUpdater) وويندوز (electron-updater) معاً.
- **إصدار تحقق للتحديث الهوائي الصامت على macOS** — هذا الإصدار هو ما ستلتقطه نسخ v1.6.2 تلقائياً للتأكد من عمل الدورة كاملة (فحص → تنزيل صامت → تحقق sha512 → تثبيت عند إعادة التشغيل). ملاحظة: نسخة v1.6.2 المثبّتة ستنزّله بصمت تام (واجهتها تسبق الودجت)؛ الودجت يظهر بدءاً من التحديثات التالية لـ1.6.3.

## [v1.6.2] — 2026-07-16 — تحديث هوائي صامت للماك (OTA)

- **تحديث هوائي صامت للماك: تنزيل بالخلفية وتثبيت عند إعادة التشغيل** — محدّث خاص بنا (`src/mac-updater.js`) يتجاوز رفض Squirrel.Mac للتوقيع الـad-hoc: يجلب `latest-mac.yml` من آخر إصدار GitHub، يقارن النسخ، ينزّل الأصل بصمت (يفضّل zip ثم dmg) مع تحقق sha512، يجهّز الحزمة الجديدة، ثم يستبدلها سكربت منفصل عند الإغلاق (استبدال + توقيع ad-hoc + إزالة quarantine + إعادة تشغيل).
- **فحص أولي بعد 15 ثانية ثم كل 6 ساعات** — بمحاذاة سلوك ويندوز؛ أوامر الفحص/التنزيل اليدوية (IPC) تمرّ بالمسار الجديد على الماك.
- **هدف zip جديد لبنية الماك** — يُبنى من الحزمة الموقّعة ويُدرج أولاً في `latest-mac.yml` لتحديثات أنظف مستقبلاً؛ يبقى dmg للتثبيت الأول وللموقع.
- ملاحظة ترقية: نسخ 1.6.0/1.6.1 المثبّتة تحتاج تثبيتاً يدوياً واحداً (DMG) للوصول إلى 1.6.2؛ بعدها تصبح الترقيات صامتة بالكامل.

## [v1.6.1] — 2026-07-16 — الجولة التعريفية الصوتية + حملة الترجمات

- **الجولة التعريفية المنطوقة (17 خطوة):** جولة إعداد جديدة مصحوبة بموسيقى خلفية وخطوات واعية بالأدوار (تتكيّف حسب دور المستخدم)، مع تموضع ذكي وفتح تلقائي للأقسام. يُشحن صوت الجولة (`tour-audio`) داخل الحزمة.
- **حملة الترجمات (i18n):** إكمال حملة التدويل وتنظيف النصوص المؤقتة (stub cleanup) عبر الواجهة.
- **تكافؤ أعمدة الشحن للتوصيل:** هجرة `20260715a` (إضافات `ADD COLUMN IF NOT EXISTS` صرفة على `sales_transactions`) تُصلح فشل «تأكيد وإرسال» لفواتير التوصيل.
- **صفر أخطاء TypeScript:** تدقيق الجاهزية الرسمي — بناء نظيف بلا أخطاء على مستوى المشروع.
- **استبعاد RLS الرولونات محلياً:** هجرة `20260716a_roll_movements_rls` مستبعَدة من النسخة المحلية (تعمل بـRLS معطّل بالتصميم).

## [v1.6.0] — 2026-07-16 — الإصدار المجمّع (Milestone)

### القيود ومستندات SME (حذف = عكس مخفي + سجل تعديلات):
- **الحذف الناعم للمستندات (مراحل 1-3 شاملة):** حذف القيد/الفاتورة/السند صار عكساً محاسبياً مخفياً بدل مسح مدمّر — يحافظ على توازن الأستاذ وأثر المراجعة، مع حارس تبعيات يمنع حذف مستند مرتبط.
- **سجل التعديلات (edit log):** تتبّع تعديلات القيود والمستندات مع إخفاء المحذوف من النشاط الأخير.
- **الرولونات والحجوزات:** توسيع الحذف الناعم للمرحلة 3 ليشمل حركات الرولونات (`roll_movements`) والحجوزات.

### الوضع الليلي (سبعة إصلاحات) وتناسق الواجهة:
- حزمة الوضع الليلي: لون `gray-850` (#1a2332) الجديد بشريط Tailwind، وسبعة إصلاحات تباين/تناسق عبر اللوحات.
- **مركز الإشعارات** المُوحّد + **تبويبات الفلاتر** الديناميكية.
- **معالج الإعداد الدائم** (onboarding): حدث `texacore:open-onboarding` لإعادة فتح المعالج، و**الوضع التلقائي للثيم** (`header.systemMode`).

### الهجرات (272-277):
- `20260714b` إشعار التثبيت المحلي بالموقع/الـIP (مُكيَّف للمُثبِّت: حارس مخطط `licensing` — دوال سحابية تُتخطّى بأمان محلياً).
- `20260714c` `check_special_permission` — أساس البيع/الشراء المباشر الخادمي.
- `20260714d` إصلاح FK استلام المشتريات (`purchase_receipts.invoice_id` → `purchase_transactions`).
- `20260714e` اشتقاق COGS للبيع المباشر (مُكيَّف: يطبّق على نسخة v3 أو يتخطّى بأمان على سلالة full-schema-sync).
- `20260715a` تكافؤ أعمدة الشحن على `sales_transactions`.
- `20260716a` تفعيل RLS على `roll_movements` (عزل tenant).

---

## [v1.5.45] — 2026-07-13

### الإصلاحات (سلامة المخزون والمحاسبة):
- **إغلاق سباق البيع الزائد (oversell):** إعادة كتابة محرّك تحديث المخزون ذرّياً (فهرس فريد + upsert) — يستحيل الآن تجاوز الرصيد عند عمليات متزامنة.
- **مناقلات ذرّية:** تأكيد المناقلة صار عملية واحدة (رولونات + حركات + حالة) لا تنفصل عند أي فشل.
- **مرتجعات فروقات الحاويات:** إنشاء مرتجع مشتريات كامل (حركات + قيد Dr مورد / Cr مخزون) بدل نائب فارغ.
- **رسملة تكلفة الشحن:** قيد محاسبي (Dr مخزون / Cr مصروف استيراد) للجزء القائم — توحيد الأستاذ المساعد مع العام.
- **إصلاحات محاسبية كامنة:** نمط ترحيل القيود (draft→posted) لتجنّب كسر قيد التوازن؛ توسيع محلّل حساب دائني المرتجعات.

---


## [v1.5.44] — 2026-07-11

### التغييرات:
- **نظام التصميم الموحّد الكامل**: 15 لوحة قسم بالشريط الكحلي الفخم + ~82 صفحة داخلية + 13 صفحة إعدادات موحّدة العرض والنمط (رأس واحد، أزرار HeroButton، بطاقات StatCard)
- **عقيدة الجداول الثلاثية**: كل قوائم البيانات على NexaListTable المُرقّى (بحث داخلي، تصدير Excel/طباعة RTL، إجماليات Σ، تحديد جماعي، إظهار/إخفاء أعمدة محفوظ)؛ جداول التفاصيل على DetailTable الموحّد
- **ثيم الرسوم البيانية**: ECharts حصراً بلوحة ألوان مُتحقَّقة (نهاري/ليلي) عبر 12+ رسماً
- **فلتر التاريخ الموحّد**: افتراضي «أول الشهر ← اليوم»، أسهم RTL صحيحة، بريسِت «كل المدة»، بريسِتات جانبية أنيقة؛ يُزال من أدلّة الكيانات (الموردين/الزبائن)
- **إصلاحات**: صفحات بيضاء بمسارات قديمة، ازدواج ترويسات، تناسق الوضع الليلي، شرائح SVG خفيفة بدل Recharts

---

## [v1.5.40] — 2026-07-02

### 🔒 أمان (P0): إزالة بيانات دخول مشرف مسرّبة كانت تُشحن مع كل نسخة
- حُذف الملف الميّت `src/api-server-standalone.js` بالكامل. كان **غير مستخدَم** (لا يستورده أي جزء من التطبيق) لكنه يُشحن ضمن الحزمة (`asar:false`) ويحوي نصّاً صريحاً:
  - كلمة مرور حساب دعم/سوبر-أدمن مسرّبة، مقترنة ببريد الأدمن — أي شخص يفتح الـDMG/الـexe كان يقرأ بيانات دخول مشرف منصّة صالحة.
  - نسخة مكرّرة من سرّ JWT وكلمة مرور القاعدة ومفاتيح anon/service المشتركة.
- **مطلوب بعد النشر:** تدوير حساب الدعم/السوبر-أدمن سحابياً (الكلمة المسرّبة يجب إبطالها في GoTrue).

### 🔄 التحديثات التلقائية الهوائية (OTA) تعمل فعلياً على ويندوز
- سابقاً: `electron-updater` كان موصولاً في العملية الرئيسية لكن **لا مستهلك في الواجهة** و`autoDownload=false` ⇒ يُكتشف التحديث ثم يُسقَط بصمت (لا تنزيل ولا إشعار).
- الآن على ويندوز: **تنزيل تلقائي في الخلفية** + **حوار أصلي «إعادة التشغيل الآن / لاحقاً»** عند جاهزية التحديث (يُثبَّت تلقائياً عند الإغلاق إن أُجّل) + **فحص دوري كل 6 ساعات** إضافةً لفحص الإقلاع.
- ماك: يبقى التحديث يدوياً عبر الـDMG (التوقيع ad-hoc، وSquirrel.Mac يرفض تحديث تطبيق غير موقّع بـDeveloper ID) — عُطِّل مسار OTA على ماك لتفادي محاولات تنزيل/تثبيت فاشلة.

### ملاحظات مؤجَّلة (تحتاج جلسة قابلة للاختبار على ويندوز)
- تصليب مصادقة القاعدة المحلية (`pg_hba` من `trust` إلى `scram-sha-256`) — يمسّ التثبيتات الجديدة فقط ويحتاج ترتيب bootstrap دقيق (تعيين كلمة مرور دور `postgres` قبل اتصال المجمّع)؛ أُجِّل لتفادي إعطاب تثبيتات ويندوز الجديدة غير القابلة للاختبار من هنا.
- توقيع/notarization حقيقي (Developer ID للماك، Authenticode لويندوز)، تفعيل `asar`، وتقاعُد سرّ JWT المشترك للتثبيتات القديمة.


## [v1.5.39] — 2026-06-27

### اتساق الموديولات بين localhost والصب-دومين (نفس المستخدم كان يرى موديولات أقل)
نفس المستخدم كان يرى موديولات **أقل** عبر الصب-دومين (`textile001.texacore.ai`) منها عبر `localhost:8080` — رغم أن **كلاهما يصل نفس الخلفية المحلية** (بروكسي `/_supabase` → Kong المحلي). جذران:
- **واجهة قديمة مخزّنة على أصل الصب-دومين:** كل أصل (localhost مقابل الصب-دومين) له **كاش PWA منفصل + كاش Cloudflare على الحافة**. بعد التحديث، التزم localhost بالواجهة الجديدة بينما بقي الصب-دومين على واجهة قديمة بقائمة موديولات أقدم. **الإصلاح:** `index.html` و`sw.js` و`manifest` تُخدَم الآن بـ`Cache-Control: no-store` ⇒ المتصفّح/Cloudflare يجلبان الأحدث دومًا ⇒ الـSW يتحدّث ⇒ تتطابق الواجهتان.
- **`SELF_HOSTED_ALLOWED` قائمة بيضاء يدوية** كانت تتوسّع عبر الإصدارات (مصدر انجراف بين النسخ القديمة/الجديدة). **أُزيلت:** التبويب المحلي يعتمد الآن على **الباقة فقط** (`hasModule`) — مصدر حقيقة واحد مطابق لـ`ModuleGuard` وللسحابة.
- **بعد التحديث:** حدّث الصب-دومين مرّة (تحديث قوي) لمسح الكاش القديم؛ بعدها تتطابق الموديولات تلقائيًا في كل مكان.


## [v1.5.38] — 2026-06-27

### حماية الوصول بالرابط: الموديولات خارج الباقة محجوبة فعليًا (ثغرة أمنية)
البوّابة كانت تُخفي الموديولات من القائمة الجانبية فقط، لكن **الوصول عبر الرابط مفتوح** — `/pharmacy`، `/exchange`، `/component-lab`، أدوات التطوير… كلها تُفتح بالرابط (المستخدم أبلغ).
- **`ModuleGuard`** كان يتجاوز البوّابة كليًا على التثبيت المحلي (`if (isSelfHosted) return null`) وللأدمن (سطر 80). **الآن بوّابة الباقة تنطبق على الجميع** (المالك المحلي + السوبر أدمن): أي موديول خارج الباقة يُعرَض كشاشة «غير مشمول في باقتك» بدل المحتوى. `/saas` يُستثنى (حارسه الخاص يحميه).
- **أدوات التطوير** (`component_lab`/`system_verify` + `/sheets-lab`,`/design-system`,`/grid-lab`…) أُزيلت من «المسموح دائمًا» وتُعامَل كـ`component_lab` ⇒ محجوبة ما لم تشملها الباقة (إنتربرايز).
- **لا fail-open أثناء التحميل:** كان كل مسار يُعرَض ريثما تُحمّل الباقة (ثغرة وميض/تسريب)؛ الآن **لودر** حتى تُحسم الباقة.
- **وميض القائمة الجانبية:** كانت تُظهر كل الموديولات لحظة التحديث ثم تُخفي؛ الآن تُخفي غير الأساسية أثناء التحميل (تنمو لا تنكمش).
- **تطبيع الرموز:** `hasModule` يطبّع الرمز (`e-commerce`↔`ecommerce`) لئلا يُحجب موديول مشروع بسبب اختلاف صياغة.
- ملاحظة: الموديولات تتبع **تعريف الباقة في `/saas`**؛ لإتاحة موديول للاحترافية أضِفه لباقة `texa-professional` هناك (ينتشر سحابيًا).


## [v1.5.37] — 2026-06-26

### إصلاح حاسم: بوّابة الموديولات تعمل فعليًا (المجاني + كل الباقات)
رغم v1.5.36، بقيت **كل الموديولات تظهر** (verticals صيدلية/مطعم/ذهب/عقار…) على تثبيت TRIAL، والمجاني بلا تقييد إطلاقًا. ثلاثة جذور:
- **`psqlExec` يعمل تحت `ON_ERROR_STOP=1`** + خطوات `syncActivePlan` الثلاث في `try` واحد ⇒ فشل **الخطوة 1** (إسناد `included_modules::jsonb` على عمود قد يكون `text[]` في تثبيت مُرقّى) كان **يُلغي الخطوتين 2+3** (التوجيه + البوّابة). الآن **كل خطوة معزولة** في try/catch خاصّ.
- **مسار المجاني في النبضة لم يكن يستدعي `syncActivePlan` إطلاقًا** ⇒ المجاني يعرض كل شيء. الآن يستدعي `syncActivePlan('free')` (tier صريح).
- **هشاشة SQL:** `ON CONFLICT (tenant_id, module_code)` يفشل لو القيد مختلف/غائب بتثبيت مُرقّى. الآن **NOT EXISTS** (بلا اعتماد على قيد)، و**`to_jsonb(included_modules)`** يقرأ العمود سواء `jsonb` أو `text[]` (مضادّ للانجراف)، وقائمة الموديولات تُؤخذ **مباشرة من السحابة** (VALUES) لا من قراءة العمود.
- **مُختبَر:** المجاني ⇒ 14 موديولًا (الـ10 + CORE) + حدود 1 مستخدم/200 فاتورة؛ الاحترافي ⇒ 25 + 10/500؛ verticals=0 في الحالتين.


## [v1.5.36] — 2026-06-26

### الباقات والحدود سحابية-مركزية (التجريبي = احترافية فعليًا)
- **المشكلة:** v1.5.35 قيّد الموديولات من `enabled_modules` بالترخيص (قائمة يدوية 6 موديولات للتجريبي)، والحدود كانت `local-unlimited` (غير محدودة) — لا تعكس الباقة. والتجريبي مُتفَّق أن يكون **بالباقة الاحترافية** كالسحابة.
- **الحل (نموذج موحّد كالسحابة متعددة المستأجرين):** مصدر الحقيقة الوحيد للموديولات **والحدود** = خطة الاشتراك حسب `tier`. دالة جديدة **`syncActivePlan`**:
  1. خريطة `tier→plan`: `trial`/`pro`→`texa-professional`، `starter`→`texa-starter`، `enterprise`→`texa-enterprise`، `free`→`free`.
  2. تسحب الباقة من `subscription_plans` السحابي (anon REST، كـ`syncFreePlanLimits`) وتُحدّث الخطة المحلية ⇒ **تعديل الأدمن للباقة في `/saas` ينتشر لكل جهاز** في نبضته التالية.
  3. توجّه اشتراك المستأجر لخطة الـtier + تزامن `tenant_modules` منها.
- **النتيجة للتجريبي (tier=pro):** `texa-professional` = **25 موديولًا** (بلا verticals صيدلية/مطعم/ذهب/عقار) + حدود فعلية (10 مستخدمين، 3 شركات، 10 مستودعات، 5000 منتج، 500 فاتورة/شهر). مُختبَر end-to-end.
- مربوطة في: نبضة المدفوع + معالجَي الاستيراد + إنشاء الشركة. مسار المجاني **لم يُمَسّ** (مستقر). التخزين يبقى محليًا (قرص المالك). **بلا أي كتابة على قاعدة الإنتاج السحابية** (قراءة anon فقط).


## [v1.5.35] — 2026-06-26

### بوّابة الموديولات حسب الباقة + مزامنة شبه-حيّة
- **الاستيراد لم يكن يزرع `tenant_modules`** ⇒ كل الموديولات تظهر بلا التزام بالباقة. الآن معالجا الاستيراد يستدعيان `syncTenantModulesFromLicense` فور إنشاء الشركة.
- **مزامنة `tenant_modules` من `enabled_modules` بالترخيص** في كل heartbeat (بعد `applyCloudState`): الموديولات المرخّصة (∪ CORE) تُفعّل والباقي يُعطّل ⇒ القائمة الجانبية تعرض موديولات الباقة فقط، وتغيير الأدمن للموديولات (في `/saas/platforms`) يَسري على كل نسخة في نبضتها التالية.
- **الواجهة شبه-حيّة:** قُلّص كاش `usePlanLimits` من 5 دقائق إلى 60 ثانية (التغيير يظهر خلال ~دقيقة دون تحديث يدوي؛ تحديث الصفحة فوري).
- CORE يبقى دائماً مُفعّلاً (لا يقفل النظام نفسه). **مُختبَر: قائمة مقيّدة ⇒ الموديولات غير المرخّصة تُعطّل فعلاً.**
- ملاحظة: موديولات كل باقة تُضبط من السحابة (الترخيص). التجريبي بكامل الموديولات بطبيعته.


## [v1.5.34] — 2026-06-26

### الحلّ الجذري النهائي لحدّ «0/0»
- **`get_all_plan_limits` يُرجع «غير محدود» عند غياب الاشتراك** بدل خطأ: في النسخة المحلية «لا اشتراك = المالك المحلي = غير محدود» (التجريبي/المستورَد بلا اشتراك). كان يُرجع `no_active_subscription` فيعرضها الفرونت 0/0 ويحجب. الآن غير محدود ⇒ **تُنشأ الفواتير**. (المجاني يبقى بـ200/شهر لأنه يحمل اشتراك free.)
- **`20260626m`** يُعيد تعريف الدالة (فرع «بلا اشتراك» → غير محدود). تريغر القاعدة `enforce_plan_limit` كان أصلاً يسمح لغير المجاني/بلا اشتراك (يُنفّذ للمجاني فقط) — فالحاجب كان الفرونت فقط.
- **مُختبَر بأداة fresh-install على مسار «بلا اشتراك» (حالة المستخدم): الحدّ مسموح (max=-1).**


## [v1.5.33] — 2026-06-26

### إصلاح حدّ الفواتير «0/0» نهائياً (للشركات المُعاد استيرادها)
- **السبب الحقيقي:** معالج الاستيراد كان يزرع الاشتراك **فقط عند إنشاء شركة جديدة**؛ وإعادة الاستيراد **تُعيد استخدام الشركة الموجودة** فيتخطّى الزرع ⇒ تبقى بلا اشتراك ⇒ «0/0» يحجب إنشاء الفواتير. نُقل الزرع **خارج فرع الإنشاء** (يعمل للإنشاء وإعادة الاستخدام، في كلا مساري الاستيراد).
- **`20260626l`**: يُعيد زرع اشتراك لأيّ مستأجر بلا اشتراك ⇒ شركتك الحالية تُصلَح **عند التحديث دون إعادة استيراد**.


## [v1.5.32] — 2026-06-26

### إصلاح طلبيات الشراء (قيد المرحلة المُنجرف)
- **`purchase_transactions_stage_check`**: كان قيد المرحلة مُنجرفاً بين النسخ (بعضها يسمح 'order' لا 'confirmed'، وبعضها العكس)، فتفشل طلبية الشراء بـ`violates check constraint`. مايجريشن `20260626k` **يُوحّد قيدَي الشراء والمبيعات على اتحاد شامل** يتجاوز أيّ حالة مُنجرفة — لا تُرفَض أيّ مرحلة مشروعة بعد الآن.
- المحوّل يستعمل مراحل متوافقة مع السحابة (طلبية = 'confirmed'، مزامنة الفاتورة بمراحل صحيحة).
- **أداة التحقّق صارت أمينة**: تُنشئ `auth.users` قبل الترحيلات (كي تُطبَّق مزامنة السكيما كاملةً) فتلتقط علل القيود التي تظهر فقط على التثبيت الحقيقي — وقد التقطت هذه العلّة.


## [v1.5.31] — 2026-06-26

### اختبار «تثبيت نظيف» شامل + إصلاحان
- **🧪 أداة تحقّق طبق الأصل (`scripts/verify/fresh-install-e2e.js`)**: تبني قاعدة نظيفة بكل المانيفست (نفس كود الويندز) ثم تُجري التدفّق الكامل (إنشاء شركة + استيراد + حدود) وتُؤكّد كل شيء. **ما ينجح فيها ينجح على ويندز حرفياً** — لكسر دورة الإصلاح واحدةً تلو الأخرى.
- **إصلاح حدّ «0/0» للشركات المستوردة**: معالج الاستيراد كان يُنشئ شركة بلا اشتراك ⇒ `get_all_plan_limits` يُرجع no_active_subscription ⇒ تعذّر إنشاء الفواتير. الآن يزرع اشتراك `local-unlimited` (كلا مساري الاستيراد).
- **تحصين طلبيات الشراء**: عزل كل طلبية بـsavepoint — فشل طلبية واحدة لا يُجهض المعاملة ويتسلسل لـ«transaction aborted»؛ والسبب الحقيقي يظهر الآن في السجلّ/التقرير بدل الخطأ العام.


## [v1.5.30] — 2026-06-26

### إكمال استيراد الرشيد (.rsf) — استُخرج السبب من سجلّ الأخطاء:
- **استيراد فواتير المشتريات**: كان المحوّل يُدرج `received_qty` فقط دون عمود `quantity_received` (NOT NULL منفصل) ⇒ تُتخطّى كل فواتير المشتريات. أُضيف العمود (= الكمية).
- **استيراد حركات المخزون**: `stock_transfers.created_by` (NOT NULL سحابي) كان فارغاً وقت الاستيراد (لا مستخدم بعد) ⇒ تُتخطّى كل الحركات. مايجريشن `20260626j` يُرخيه (بيانات تاريخية).
- **طلبيات الشراء**: كانت تتسلسل من فشل الفواتير ⇒ تعمل الآن بعد إصلاح الفواتير.
- **عدّاد دقيق للحركات**: التقرير كان يعرض عدد المناقلات (1) بينما الحركات الفعلية 11 ⇒ صار يعرض الإجمالي الحقيقي.
- **تأكيد الاكتمال**: لا بيانات مفقودة — الفجوات الظاهرة مشروعة (65 قيداً فارغاً بلا سطور، طلبية شراء واحدة تحوّلت لفاتورة، حسابات نظام قليلة).


## [v1.5.29] — 2026-06-26

### إصلاحات حرجة (إنشاء شركة جديدة + حدود الباقات):
- **إصلاح الشركة الجديدة الناقصة**: كانت الشركة تُنشأ بشجرة حسابات فقط بلا صفّ `company_accounting_settings` ولا مستودع افتراضي ⇒ أخطاء 406 بكل الشاشات وتعذّر إنشاء الفواتير. المعالج الآن يستدعي `setup_company_defaults` (إعدادات + مستودع + فرع) ثم `auto_set_default_accounts` (ربط الحسابات الافتراضية) ويُزامن العملات المختارة.
- **إصلاح تريغر إنشاء الشركة المكسور**: `trg_on_company_created` كان يستدعي دالة غير موجودة (`create_simple_chart_of_accounts`) فيُفشِل أي إدراج شركة بتريغرات مُفعّلة — أُسقِط (مطابقةً للسحابة).
- **إصلاح حدود الباقات «0/0»**: (أ) جدول `subscription_plans` كان فارغاً في التثبيت ⇒ زُرعت الباقات الأربع (free=200 فاتورة/شهر، المدفوعة 500) + باقة `local-unlimited`. (ب) `get_all_plan_limits` كان ينفجر بـ`COALESCE text[]/jsonb` (عمود `included_modules` jsonb محلياً) ⇒ أُصلح. (ج) المعالج يُنشئ اشتراكاً لكل تثبيت (مجاني→free، تجريبي/مدفوع→غير محدود) بدل المجاني فقط.
- **إصلاح الشركات الموجودة**: مايجريشن يُصلح أي شركة/مستأجر أُنشئ قبل هذا الإصدار (إعدادات + مستودع + اشتراك) دون إعادة إنشائها.


## [v1.5.28] — 2026-06-26

### إصلاحات حرجة (استيراد الرشيد .rsf):
- **إصلاح `savepoint sp_inv does not exist` وفشل الاستيراد كاملاً**: كل مرحلة استيراد صارت معزولة في savepoint خاص بها مع تعافٍ — فشل مرحلة لا يُجهض الاستيراد كله بعد الآن (كان فشل مرحلة مبكرة يتسلسل ويُسقِط الباقي).
- **إصلاح فقدان الفواتير/الحركات عند الاستيراد**: تعطيل المُحفّزات صار عبر `session_replication_role = replica` (موثوق) بدل `DISABLE TRIGGER ALL` لكل جدول، الذي كان قد يفشل بصمت على بعض الأجهزة فتبقى مُحفّزات تُفشِل إدراج فواتير المبيعات/المشتريات والحركات.
- **سجلّ أخطاء الاستيراد**: تُكتب أسباب تخطّي أي قسم في `last-import-errors.log` بمجلد بيانات التطبيق، وتظهر أيضاً في تقرير الاستيراد — لتشخيص أي نقص بدقّة دون تخمين.

### الباقة المجانية (مُضمَّن):
- النسخة المحلية تعرض موديولات الباقة فقط (المجاني = موديولاته الخاصة، لا كل الموديولات).


## [v1.4.2] — 2026-06-24

### إصلاحات حرجة (ملف العمل .tcdb):
- **إصلاح فقدان البيانات عند فتح الملف**: الاستعادة كانت تُفرّغ القاعدة ولا تُعيد تحميلها (معاملة psql واحدة تُجهَض عند «الجدول موجود») → إزالة `--single-transaction` فتُحمَّل كل البيانات.
- **إصلاح تعذّر الدخول بعد فتح الملف**: خطوة استعادة مصادقة مخصّصة تُحمّل `auth.users`+`auth.identities` بشكل موثوق (الدخول يعمل على أي جهاز).
- **مفتاح تشفير موحّد ثابت**: ملف الـ.tcdb يفتح على أي جهاز فيه البرنامج ويصمد عبر تغيّر الترخيص؛ والملفات القديمة تبقى قابلة للفتح (محاولة مفاتيح متعدّدة عند فكّ التشفير).
- **النسخ الاحتياطي كل دقيقة** (بدل 5 د) + نسخة عند الإغلاق/بعد الاستيراد.
- إصلاح ربط حركات المبيعات بقيدها في استيراد الرشيد (RSF).

---


## [v1.4.1] — 2026-06-23

### التغييرات:
- تحديث الواجهة الكاملة (يونيو): إصلاح المتجر (خطأ 400 بالمفضّلة + رصيد العميل المسجّل)
- تسريع فتح شيتات الأطراف/الحسابات (كاش + getSession بلا شبكة)
- توحيد شجرة الحسابات الموسّعة + كشف الحساب (تطبيق على إنشاء الشركة)
- تحسينات الكاش والعرض الفوري عبر الأقسام
- إطفاء Realtime محلياً (كونسول نظيف؛ الإنتاج السحابي يبقى متزامناً)

---


## [v1.3.3] — 2026-05-08

### التغييرات:
- نظام استيراد ذكي مع ترقيم هيكلي + تعرّف تلقائي على العملات + فلترة البيانات الفارغة

---


## [v1.3.1] — 2026-05-04

### إصلاح استيراد الفواتير + تتبع الإصدارات:
- إصلاح `rsf-reader`: دعم البنية البديلة (MoveBayBill+MOVE) لفواتير المشتريات والمبيعات
- إصلاح `rsf-mapper`: إضافة خرائط ربط عكسية (`supplierByAccountMap`, `customerByAccountMap`)
- إضافة نظام تتبع الإصدارات: عرض رقم الإصدار + تاريخ البناء في Footer
- إضافة `SOURCE_OF_TRUTH.sh` كمرجع رسمي للمشروع

---

## [v1.3.0] — 2026-05-03

### النسخة الهجينة — تحول جذري من Docker إلى خدمات مدمجة:
- ✅ Embedded PostgreSQL + GoTrue + PostgREST (بدون Docker)
- ✅ نظام استيراد/تصدير الرشيد (RSF Reader/Mapper/Exporter)
- ✅ نظام النسخ الاحتياطي المشفر (.tcdb)
- ✅ نظام التراخيص المشفر (Hardware-bound)
- ✅ Cloud Access عبر Cloudflare Tunnel
- ✅ Service Manager لإدارة جميع الخدمات محلياً
- ✅ Migration Runner تلقائي

---

## [v1.0.6] — 2026-04-24

### التغييرات:
- إصلاح عرض مسار Docker وزر فتح المجلد

---


## [v1.0.5] — 2026-04-24

### التغييرات:
- إضافة زر فتح مجلد التحميل كـ fallback أخير

---


## [v1.0.4] — 2026-04-24

### التغييرات:
- إصلاح فتح مثبّت Docker على Windows — 3 طرق fallback

---


## [v1.0.3] — 2026-04-24

### التغييرات:
- إصلاح عرض رقم الإصدار ديناميكياً

---


## [v1.0.2] — 2026-04-24

### التغييرات:
- إصلاح التثبيت التلقائي لـ Docker على Windows

---


## [v1.0.1] — 2026-04-24

### التغييرات:
- Desktop Setup Wizard + Multi-Company + Backup Engine + Google Drive

---

## [v1.0.0] — 2026-04-23

### الإصدار الأول 🎉
- ✅ إنشاء برنامج التثبيت (Electron + electron-builder)
- ✅ بناء macOS (.dmg Universal) و Windows (.exe NSIS)
- ✅ نظام التراخيص (Trial + Paid) مع تفعيل تلقائي
- ✅ اكتشاف Docker تلقائياً مع تحميل تلقائي إذا غير مثبت
- ✅ System Tray — يعمل في الخلفية مثل Google Drive
- ✅ نظام التحديثات التلقائية (electron-updater + GitHub Releases)
- ✅ إرسال مفتاح الترخيص بالبريد الإلكتروني
- ✅ إشعار انتهاء الترخيص (Banner تحذيري قبل 7 أيام)
- ✅ أيقونة التطبيق من شعار TexaCore الأصلي

---
